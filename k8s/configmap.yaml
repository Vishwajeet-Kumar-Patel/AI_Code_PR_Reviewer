apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: ai-code-reviewer
data:
  ENVIRONMENT: "production"
  LOG_LEVEL: "INFO"
  CORS_ORIGINS: '["https://app.ai-code-reviewer.com"]'
  MAX_WORKERS: "4"
  RATE_LIMIT_ENABLED: "true"
